<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.daiict.enterprizecomputing.reviewdekho">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.ReviewDekho">
        <activity android:name=".SystemDashboard.SystemDashboard"></activity>
        <activity android:name=".DashboardLogin.HowWeWork" />
        <activity android:name=".DashboardLogin.DashboardLogin" />
        <activity android:name=".SignUp.SignUp" />
        <activity android:name=".Login.ForgotPassword" />
        <activity android:name=".Login.Login" />
        <activity android:name=".IntroSlider.IntroSlider" />
        <activity android:name=".SplashScreen.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>